
    set autoindent " Turn on autoindent 
    set expandtab "Tell vim to put spaces when I hit <TAB>
    set tabstop=4 " Tell vim how many spaces to show when it sees <TAB> 
    set softtabstop=4 " How many spaces count as a <TAB> when editing
    set shiftwidth=4 " How many spaces when indenting <greater than x2> 
    
    set foldmethod=indent "Turn on code folding and set the method to by indentation
    set nofoldenable "Set default to be unfolded when opening files


    set laststatus=2 "Set status line to always be on
    set number "Turn on line numbers
    
